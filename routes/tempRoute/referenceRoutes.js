// const express = require('express');
// const router = express.Router();
// const authMiddleware = require('../middlewares/authMiddleware');
// const roleMiddleware = require('../middlewares/roleMiddleware');

// const { getReferences, addReferences, deleteReference, updateReference } = require('../controllers/referenceController');

// router.get('/', authMiddleware, roleMiddleware(['admin', 'receptionist']), getReferences);
// router.post('/add-ref', authMiddleware, roleMiddleware(["admin"]), addReferences);
// router.delete('/delete-ref:id', authMiddleware, roleMiddleware(["admin"]), deleteReference);
// router.put('/update-ref:id', authMiddleware, roleMiddleware(["admin"]), updateReference);



// module.exports = router;
